# منصة مدرسة أزاد الإلكترونية
## خطة المشروع الشاملة — وثيقة مرجعية (معتمدة)

**المشروع:** منصة تعليم إلكتروني لطلاب فلسطين (مناهج فلسطينية) — صفوف، دروس، اختبارات، درجات، واجبات، حضور، دفع اشتراكات.
**الإصدار:** Azad First Edition — أزاد للأنظمة الذكية | تطوير: أحمد غنام
**التقنية المقررة (مثبّتة، لا تُغيَّر إلا بقرار صريح):** Python + Flask + Jinja2 + PostgreSQL
**الدفع:** يدوي أولاً (تحويل/واتساب) → بوابة (Stripe/PayPal) في مرحلة لاحقة.

## القرارات النهائية (بتاريخ اعتماد الخطة — تُحدّث هذه الوثيقة فقط بقرار صريح)
1. **Git:** مستودع محلي + رفع إلى GitHub خاص على حساب AbuAzad2025.
2. **اسم المنصة:** "منصة مدرسة أزاد الإلكترونية".
3. **البداية التجريبية (M1/M2):** الرياضيات — الصف السابع.
4. **نظام الاشتراك:** **فصلي (فصل أول / فصل ثاني) + سنوي (الفصلان معاً)** — لكل صف اشتراكه الخاص، والمواد الاختيارية لها اشتراك/مزايا منفصلة داخل الصف نفسه.
5. **نطاق المحتوى:** جميع المواد + جميع الصفوف + مواد اختيارية، بصيغ: **فيديو + نص + ذكاء اصطناعي + صور/رسومات + معلم افتراضي**.
6. **الدروس الخصوصية (T):** سوق حر داخل المنصة **خارج عزل المدرسة** — لا يشترط انتماء الطالب لمدرسة المعلم. أي معلم يُدرّس أي طالب 1-1. الدفع **يدوي أولاً** (متوافق مع D12)، الجلسة **أونلاين أو حضوري**، التسعير **سعر/ساعة + سعر جلسة**، اكتشاف المعلم عبر **بحث/صفحة عامة + دعوة برمز**. (أُقرّت بجلسة مراجعة — تُنفَّذ كمقطع موازٍ من M2.)

---

## 1. الأهداف
- منصة تعليمية متكاملة تشبه "المدرسة الإلكترونية": معلمون ينشئون صفوفاً ودروساً واختبارات، طلاب ينضمون ويحلّون ويتابعون درجاتهم.
- دعم المنهاج الفلسطيني (عربي + إنجليزي).
- ربح عبر اشتراكات الطلاب (دفع يدوي أولاً، بوابة لاحقاً).
- تُباع كخدمة white-label لمدارس ومراكز تعليمية فلسطينية.

## 2. الأدوار والصلاحيات (RBAC)
| الدور | الصلاحيات |
|---|---|
| **Super Admin (أزاد)** | كل شيء: إدارة المدارس، المستخدمين، الاشتراكات، التقارير |
| **School Admin (مدير المدرسة)** | إنشاء معلمين/صفوف/دورات، اعتماد المدفوعات، تقارير |
| **Teacher (معلم)** | إنشاء صفوف، دروس، اختبارات، واجبات، درجات، حضور |
| **Student (طالب)** | انضمام لصف (رمز)، مشاهدة دروس، حل اختبارات/واجبات، درجاته |
| **Parent (ولي أمر)** | متابعة درجات/حضور/دفع لابنه (قراءة فقط) |

## 3. الميزات الوظيفية (F1..Fn)
### الملف الشخصي والحسابات
- F1 تسجيل/تسجيل دخول (معلم/طالب/ولي أمر) + تفعيل بريد إلكتروني + إعادة تعيين كلمة مرور.
- F2 ملف شخصي: صورة، أسماء (عربي/إنجليزي)، مدرسة، صف.
- F3 لغتان: عربي (RTL) + إنجليزي (LTR)، مبدّل في كل صفحة.

### المدارس والصفوف
- F4 إنشاء مدرسة (اسم، نطاق فرعي اختياري، سنة دراسية، مراحل: ابتدائي/إعدادي/ثانوي).
- F5 صفوف داخل المدرسة: اسم، صف منوي (1-12)، مادة، معلم، رمز انضمام للطلاب.
- F6 انضمام طالب لصف عبر رمز أو دعوة من المدير.
- F7 فصل/إعادة ضم طالب، نقل صف.

### الدروس والمحتوى
- F8 درس: عنوان، وصف، محتوى (نص منسّق)، مرفقات (PDF/صور/فيديو/رابط يوتيوب).
- F9 ترتيب الدروس وتمييز المنهاج (وحدة/درس/رقم صفحة).
- F10 تقييد الوصول: دروس مفتوحة فقط لأعضاء الصف.

### الاختبارات
- F11 إنشاء اختبار: عنوان، مدة، محاولات مسموحة، وقت مفتوح/مغلق، خلط أسئلة.
- F12 أنواع الأسئلة: اختيار من متعدد، صح/خطأ، مقالي، توصيل (بداية: أول 3 أنواع).
- F13 تصحيح آلي (اختياري/صح-خطأ) + تصحيح يدوي (مقالي).
- F14 نتيجة الطالب فورية أو بعد اعتماد المعلم، ونطاق محاولات.

### الواجبات والتقييم
- F15 واجب: وصف، مرفقات، موعد تسليم، درجة قصوى.
- F16 تسليم الطالب (نص + ملف)، رفض/استقبال، تصحيح يدوي مع ملاحظات.

### الدرجات والتقارير
- F17 دفتر درجات (معلم): لكل صف/مادة جدول درجات.
- F18 بطاقة درجات للطالب: متوسطات، رتبة، توزيع الدرجات.
- F19 تقارير إدارية: حضور، درجات، إيرادات، نشاط.

### الحضور
- F20 تسجيل حضور يومي للصف (حاضر/غائب/متأخر/عذر).

### الاشتراك والدفع
- F21 اشتراك الطالب في صف/مادة: **فصلي (فصل أول / فصل ثاني) أو سنوي (الفصلان)** — خيارات أسعار لكل صف ولكل مادة اختيارية.
- F22 دفع يدوي: يطلب الطالب اشتراكاً → يسجّل ولي الأمر عملية دفع (رقم مرجع تحويل) → المدير/المدرّب يعتمد → يُفعَّل الاشتراك بفترة محددة.
- F23 تنبيه قبل انتهاء الاشتراك، وتعطيل وصول عند انتهائه.
- F24 (لاحقاً) بوابة Stripe/PayPal.

### التواصل والإشعارات
- F25 إعلانات الصف (معلم → صف).
- F26 إشعارات داخلية: نتيجة اختبار، واجب جديد، اشتراك معتمد.

### الذكاء الاصطناعي والمعلم الافتراضي (مرحلة لاحقة — تُخطط مسبقاً)
- F27 **معلم افتراضي** (مساعد AI مقيّد بمنهاج الصف): يشرح النقاط الصعبة من محتوى الدرس نفسه.
- F28 مولد أسئلة/اختبارات من نص الدرس (AI) — لمساعدة المعلم في بناء الاختبارات.
- F29 مساعد تصحيح للمقالي (اقتراح درجات/ملاحظات) — بقرار نهائي من المعلم.
- F30 تنسيق RTL كامل ومخرجات بلغة عربية سليمة لكل ميزات AI.

### الدروس الخصوصية (سوق حر — خارج عزل المدرسة)
> **استثناء تينانتس مقصود:** جداول T بلا `school_id` — حاجز العزل فيها هو **المنصة** نفسها، والوصول بصلاحية: طرفا الجلسة فقط + super_admin. (يُوثَّق في DATABASE_SCHEMA.md §3.15)
- F31 **ملف المعلم الخصوصي**: صفحة عامة قابلة للبحث (مادة/صف/سعر/أونلاين-حضوري/جدول توفر) + مفتاح دعوة برمز.
- F32 **طلب الحجز**: يبحث الطالب → يرسل طلباً → يقبل المعلم (أو يرفض/يقترح وقتاً آخر).
- F33 **جلسة الدرس الخصوصي**: زمن محدد، حالة (requested/accepted/completed/cancelled)، سعر/ساعة أو سعر/جلسة، رابط أونلاين أو مكان حضوري، تسجيل دفع يدوي.
- F34 **الوضع الحر**: لا يشترط اشتراك الطالب أو عضويته في أي صف — حساب واحد يكفي لأي دور.

## 4. الميزات غير الوظيفية (N1..Nn)
- N1 أمان: كلمات مرور bcrypt/argon2، CSRF، جلسات آمنة، صلاحيات على كل route، تجهيز SQL عبر ORM فقط.
- N2 منع رفع ملفات ضارة (قائمة امتدادات مسموحة + حد حجم + توليد أسماء عشوائية).
- N3 RTL سليم وواجهة متجاوبة (جوال أولاً — الطلاب على هواتفهم).
- N4 نسخ احتياطي PostgreSQL يومي (pg_dump).
- N5 أداء: فهرسة، استعلامات صافية، تجزئة الصفحات.
- N6 سجل تدقيق (من أنشأ/عدّل ماذا ومتى).
- N7 توافق ألوان الهوية البصرية لأزاد (النمط 135deg #0EA5E9→#14B8A6، navy #0F172A).

## 5. نموذج البيانات (PostgreSQL) — مسوّدة أولية
- **users** (id, role, email, password_hash, name_ar, name_en, avatar, school_id, is_active, created_at)
- **schools** (id, name, domain, academic_year, stages, admin_id)
- **classes** (id, school_id, name, grade_level, subject, teacher_id, join_code, is_active)
- **class_members** (id, class_id, user_id, joined_at, status)
- **lessons** (id, class_id, title, body_html, unit, sort_order, created_by, created_at)
- **lesson_attachments** (id, lesson_id, filename, stored_name, mime, size)
- **assignments** (id, class_id, title, body, due_at, max_mark, created_by)
- **submissions** (id, assignment_id, student_id, body, file, submitted_at, mark, feedback)
- **quizzes** (id, class_id, title, duration_min, attempts_allowed, open_at, close_at, shuffle, created_by)
- **questions** (id, quiz_id, type, prompt, options_json, correct_answer_json, mark)
- **quiz_attempts** (id, quiz_id, student_id, started_at, submitted_at, score, status)
- **answers** (id, attempt_id, question_id, answer_json, is_correct, awarded_mark)
- **attendance** (id, class_id, student_id, date, status)
- **grades** (id, class_id, student_id, subject, term, type, mark, max_mark, recorded_by, note)
- **subscriptions** (id, user_id, class_id, plan: first_term/second_term/annual, price, currency, start_at, end_at, status, method)
- **manual_payments** (id, subscription_id, reference, note, status, reviewed_by, reviewed_at)
- **announcements** (id, class_id, author_id, title, body, created_at)
- **audit_logs** (id, user_id, action, entity, entity_id, detail, ip, created_at)
- **tutor_profiles** (id, tutor_id, subject, grade_levels, price_hour, price_session, mode, availability, invite_code, is_active)
- **tutoring_requests** (id, tutor_id, student_id, subject, preferred_time, mode, price_quote, note, status)
- **tutoring_sessions** (id, request_id, tutor_id, student_id, subject, scheduled_at, duration_min, price, currency, mode, online_link, location, status, payment_status)

## 6. الخطة المرحلية (Milestones M0..M8)
### M0 — التأسيس (لا كود وظيفي)
- بيئة افتراضية venv، requirements.txt، بنية مجلدات، إعداد Git (مستودع جديد خاص)، README، ملف .env.example.
- مكدس: Flask, Flask-SQLAlchemy, Flask-Login, Flask-WTF, Babel (ترجمة), Jinja2, psycopg2, gunicorn.
- فحص: `pip install` نظيف + اتصال PostgreSQL يعمل.

### M1 — المصادقة والأدوار
- تسجيل/دخول/تسجيل خروج، تفعيل بريد، إعادة تعيين، صلاحيات RBAC.
- **بداية المحتوى التجريبي: الرياضيات — الصف السابع (يُبنى قالب المحتوى عليها).**
- فحص: اختبارات وحدة للجلسات والصلاحيات + تسجيل دخول يعمل فعلياً.

### M2 — المدارس والصفوف
- إنشاء مدرسة، صفوف، رموز انضمام، انضمام الطلاب.
- فحص: مدرس ينشئ صفاً وطالب ينضم برمز.

### M2T — الدروس الخصوصية (مقطع موازٍ، يبدأ مع M2)
- جداول T (§3.15) + ملف معلم خصوصي عام + بحث + طلب حجز + جلسة + دفع يدوي.
- فحص: طالب خارج مدرسة المعلم يحجز درساً ويُسجَّل دفعُه.

### M3 — الدروس والمحتوى
- دروس + مرفقات + وصول مقيّد.
- فحص: رفع ملف والوصول الآمن إليه.

### M4 — الاختبارات
- إنشاء اختبار، أنواع أسئلة، تصحيح آلي، محاولات، نتائج.
- فحص: طالب يحل اختباراً وتُحسب النتيجة.

### M5 — الواجبات والدرجات والحضور
- واجبات، تسليمات، دفتر درجات، بطاقة درجات، حضور.
- فحص: دورة كاملة (دروس→اختبار→واجب→درجة→تقرير).

### M6 — الاشتراكات والدفع اليدوي
- اشتراكات، طلبات دفع، اعتماد يدوي، تعطيل عند الانتهاء.
- فحص: ولي أمر يسجّل دفعاً والمدير يعتمده فيُفعَّل.

### M7 — اللغتان والواجهة والجودة
- Babel عربي/إنجليزي + RTL، تحسينات جوال، سجل تدقيق، نسخ احتياطي.
- فحص: اختبار شامل عبر المراحل كلها.

### M8 — النشر والانطلاق
- gunicorn + نظام خدمة، HTTPS، قاعدة إنتاج، بيانات أولية (مدارس تجريبية).
- فحص: نشر على خادم حقيقي والمنصة تعمل من الإنترنت.

### M9 (لاحقاً) — بوابة الدفع + ميزات متقدمة
- Stripe/PayPal، تقارير متقدمة، إشعارات بريد/واتساب، تسجيل فيديو.

## 7. الضوابط الصارمة للمشروع (لا يُخالف شيء منها)
- **D1** لا سطر كود وظيفي قبل اعتماد هذه الوثيقة والخطة.
- **D2** لا تغيير في التقنية (Python/Flask/Jinja/PostgreSQL) إلا بقرار صريح.
- **D3** كل المراحل تعتمد الفحص: لا ننتقل من مرحلة لمرحلة قبل فحصها وموافقتك.
- **D4** لا كلمات مرور/مفاتيح في الكود — كلها في .env (و.env مستبعد من Git).
- **D5** كل الوصول للقاعدة عبر ORM/استعلامات معقّمة فقط، لا SQL مكتوب يدوياً.
- **D6** الصلاحيات تُفحص على كل route (لا نثق بواجهة فقط).
- **D7** الملفات المرفوعة: قائمة بيضاء بالامتدادات + حد الحجم + تخزين خارج المجلد العام + اسم عشوائي.
- **D8** اللغة الافتراضية عربي (RTL) ويدعم التبديل لإنجليزي.
- **D9** اختبارات (pytest) لكل مرحلة قبل اعتمادها.
- **D10** Git: commits صغيرة وواضحة، وفرع main محمي، وبدون ملفات كبيرة/مخزونات في الريبو.
- **D11** النسخ الاحتياطي اليومي للقاعدة بمجرد دخول M6.
- **D12** لا دفع فعلي/بوابة قبل M9 وموافقة صريحة.

## 8. القرارات المعتمدة (بديل التساؤلات السابقة — سُجّلت أعلى الوثيقة)
| السؤال | القرار |
|---|---|
| مستودع Git؟ | GitHub خاص + محلي (حساب AbuAzad2025) |
| اسم المنصة؟ | منصة مدرسة أزاد الإلكترونية |
| مادة البداية التجريبية؟ | الرياضيات — الصف السابع |
| نظام الاشتراك؟ | فصلي (فصل أول/ثاني) + سنوي؛ لكل صف اشتراكه، والمواد الاختيارية بمزايا منفصلة |
| نطاق المحتوى؟ | جميع المواد/الصفوف + فيديو + نص + AI + صور/رسومات + معلم افتراضي |
| الدروس الخصوصية؟ | سوق حر خارج عزل المدرسة — دفع يدوي، أونلاين/حضوري، سعر/ساعة+جلسة، بحث+دعوة برمز (M2T) |

## 9. مراجع البحث (مصادر استُلهم منها التصميم)
- **OpenEduCat** (openeducat.org، github.com/openeducat): نموذج وحدات التعليم ERP (student/faculty/course/timetable/attendance/exam/fees/gradebook) + حقيبة الرسوم/الأقساط المستقلة — مرجع نموذج البيانات.
- **NOW-LMS** (github.com/bmosoluciones/now-lms — Flask+Bootstrap، Apache-2.0): تطابق المكدس + 880+ اختبار آلي + أدوات تحقيق دخل مدمجة — مرجع الجودة والاختبارات والربح.
- **vmse10/Quiz-App** و **prajwalsprajwal/Flask-LMS-Exam-System**: بنية سؤال/امتحان/محاولة في Flask+PostgreSQL.
- **أطروحة Online Exam Database System (Flask+PostgreSQL)**: أسئلة عشوائية، مؤقّت عدّ تنازلي، كشف تبديل الشاشة، تصحيح آلي.
- **Wsh7Ash/arabic-elearning-platform** و **Motqn.ai** و **iTutor**: معلم AI عربي + تصحيح عربي آلي + توليد أسئلة من المنهاج — مرجع ميزات F27-F30.

---
**الخطوة التالية:** اعتماد الوثيقة أعلاه → بدء **M0** (venv، requirements.txt، بنية المجلدات، Git محلي + ريبو GitHub خاص، README، .env.example، الاتصال بـ PostgreSQL).
